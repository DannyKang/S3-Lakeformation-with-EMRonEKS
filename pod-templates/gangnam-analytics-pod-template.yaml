apiVersion: v1
kind: Pod
metadata:
  name: gangnam-analytics-driver
  namespace: emr-data-team
  labels:
    app: gangnam-analytics-analysis
    role: LF_GangnamAnalyticsRole
    lake-formation: enabled
spec:
  serviceAccountName: emr-gangnam-analytics-sa
  containers:
  - name: spark-kubernetes-driver
    resources:
      requests:
        memory: "1Gi"
        cpu: "1"
      limits:
        memory: "2Gi"
        cpu: "2"
    env:
    - name: AWS_REGION
      value: "ap-northeast-2"
    - name: LAKE_FORMATION_ENABLED
      value: "true"
    - name: SESSION_TAG_VALUE
      value: "EMRonEKSEngine"
  restartPolicy: Never
